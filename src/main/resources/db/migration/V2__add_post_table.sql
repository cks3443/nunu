CREATE TABLE post
(
    id           BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    created_date TIMESTAMP WITHOUT TIME ZONE,
    update_date  TIMESTAMP WITHOUT TIME ZONE,
    active       BOOLEAN,
    title        VARCHAR(255),
    CONSTRAINT pk_post PRIMARY KEY (id)
);

ALTER TABLE sub
    ADD post_id BIGINT;

ALTER TABLE sub
    ADD CONSTRAINT FK_SUB_ON_POST FOREIGN KEY (post_id) REFERENCES post (id);